{
  "$schema": "https://awesome-rules.invalid/factory/mutations-v0.json",
  "description": "S1 缺陷集（wop-python-sdk 本地化）：篡改类（guard 门）+ 行为破坏类 B-1xx（tests 门 = run_tests.sh --no-lock → pytest tests + .factory/tests 双套件）。行为类 kill 证据 = 注入后对应 pytest 套件变红；负例（expect_block=false）验证门不全拒。证据留档 EVIDENCE-2026-08-24.md（文件名沿用 run.py write_stamp 默认值，内容为本仓 2026-08-29 首装重证）。",
  "defects": [
    {
      "id": "G-01",
      "description": "篡改治理宪法：MISSION.md 铁律降级为可选建议",
      "target": "MISSION.md",
      "find": "## 铁律",
      "replace": "## 铁律（工厂可自行修订）",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "G-02",
      "description": "篡改贡献指南：CONTRIBUTING.md 标题注入豁免条款",
      "target": "CONTRIBUTING.md",
      "find": "# 贡献指南",
      "replace": "# 贡献指南（工厂豁免条款适用）",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "G-03",
      "description": "篡改验证门自身：guard.py 周界配置载入改向不存在文件致门 fail-closed（exit 2 应视为拦截）",
      "target": ".factory/guard.py",
      "find": "cfg_path = Path(__file__).resolve().parent / \"factory-local.json\"",
      "replace": "cfg_path = Path(__file__).resolve().parent / \"factory-local-DISABLED.json\"",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "G-04",
      "description": "篡改质检线：run_tests.sh 移除 pipefail 削弱真实退出码传播",
      "target": "scripts/run_tests.sh",
      "find": "set -u -o pipefail",
      "replace": "set -u",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "M-01",
      "description": "周界清单单边漂移：MISSION 增加条目而 guard 副本未同步（self_check 应发现不一致）",
      "target": "MISSION.md",
      "find": "- 治理：`MISSION.md`、`README.md`、`README.en.md`、`CONTRIBUTING.md`",
      "replace": "- 治理：`MISSION.md`、`README.md`、`README.en.md`、`CONTRIBUTING.md`、`factory-extra/`",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "M-02",
      "description": "周界条目指向不存在路径：MISSION 改名而实际文件未跟随（self_check 存在性校验应拦截）",
      "target": "MISSION.md",
      "find": "`src/wop_sdk/sm4gcm.py`",
      "replace": "`src/wop_sdk/sm4gcm-renamed.py`",
      "gate": "guard",
      "expect_block": true
    },
    {
      "id": "B-01",
      "description": "负例：周界外文档篡改（docs/ 不在 PERIMETER）——验证 guard 门不全拒",
      "target": "docs/mutation-report.md",
      "find": "# 变异测试报告（wop-python-sdk）",
      "replace": "# 变异测试报告（wop-python-sdk，工厂参与维护）",
      "gate": "guard",
      "expect_block": false
    },
    {
      "id": "B-101",
      "description": "破坏密钥边界合同：SM2 私钥上界 d < n 放宽为 d <= n（D12/I5：d == n 必须被拒）",
      "target": "src/wop_sdk/keys.py",
      "find": "    if not 1 <= d < _SM2_N:",
      "replace": "    if not 1 <= d <= _SM2_N:",
      "gate": "tests",
      "expect_block": true
    },
    {
      "id": "B-102",
      "description": "破坏配置不可变契约：WopConfig frozen 解除（API 契约：配置防运行中篡改，FrozenInstanceError 必抛）",
      "target": "src/wop_sdk/client.py",
      "find": "@dataclass(frozen=True)",
      "replace": "@dataclass(frozen=False)",
      "gate": "tests",
      "expect_block": true
    },
    {
      "id": "B-103",
      "description": "破坏 SM2 验签：错误签名恒放行（verify 判定短路）",
      "target": "src/wop_sdk/signature.py",
      "find": "    if not sm2_verify_with_sm3(verifier, signature.hex(), data):",
      "replace": "    if False:",
      "gate": "tests",
      "expect_block": true
    },
    {
      "id": "B-104",
      "description": "破坏工厂反馈循环：rejected 标签清理 while 循环短路（.factory/tests/test_factory_lib 应红）",
      "target": ".factory/factory_lib.py",
      "find": "    while \"[factory:rejected]\" in text:",
      "replace": "    while False:",
      "gate": "tests",
      "expect_block": true
    },
    {
      "id": "B-105",
      "description": "破坏摘要头合同：alg 与 hex 间恰一空格变双空格（D2/F4：build/check roundtrip 必炸）（锚点随 236f736 Sourcery f-string 化同步，变异语义不变：恰一空格→双空格）",
      "target": "src/wop_sdk/digest.py",
      "find": "    return f\"{suite.digest_tag} {compute_digest(suite, body).hex()}\"",
      "replace": "    return f\"{suite.digest_tag}  {compute_digest(suite, body).hex()}\"",
      "gate": "tests",
      "expect_block": true
    },
    {
      "id": "B-106",
      "description": "负例：docstring 措辞变更（行为零投影）——tests 门不应拦截",
      "target": "src/wop_sdk/digest.py",
      "find": "\"\"\"组装 `<alg> <小写hex>`（恰一空格）。\"\"\"",
      "replace": "\"\"\"组装 `<alg> <小写hex>`（恰一空格，负例：仅措辞变更）。\"\"\"",
      "gate": "tests",
      "expect_block": false
    },
    {
      "id": "D-01",
      "description": "删除对外 API docstring：load_rsa_public_key 公钥解析契约说明被抹（docstring 门应对外 100%）",
      "target": "src/wop_sdk/keys.py",
      "find": "def load_rsa_public_key(material: str, expected_bits: Optional[int] = None) -> rsa.RSAPublicKey:\n    \"\"\"解析 RSA 公钥（SPKI DER，D12 材料串）；expected_bits 给定时校验长度与套件一致。\"\"\"\n",
      "replace": "def load_rsa_public_key(material: str, expected_bits: Optional[int] = None) -> rsa.RSAPublicKey:\n",
      "gate": "docstring",
      "expect_block": true
    },
    {
      "id": "D-02",
      "description": "删除对外 API docstring：HttpxTransport.send 流式限量语义被抹（docstring 门应对外 100%）",
      "target": "src/wop_sdk/transports/httpx_transport.py",
      "find": "        \"\"\"httpx 流式执行请求：iter_bytes 逐块经 read_capped 限量后归一为 HttpResponse。\"\"\"\n        with self._client.stream(\n",
      "replace": "        with self._client.stream(\n",
      "gate": "docstring",
      "expect_block": true
    }
  ]
}
