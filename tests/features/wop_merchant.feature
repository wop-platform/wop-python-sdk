# language: zh-CN
# 规格来源: wop-specs/sdk/wop-sdk-spec.md（F1-F9、D1-D5、I1-I7）
# 场景分析: docs/scenario-analysis.md（S1-S10 → 本文件 20 场景）
功能: 商户接入 WOP 网关全旅程
  商户使用官方 SDK 完成: 套件配置(F1) → 出向 L0/L2 请求(F2-F5/F9)
  → 平台响应/回调校验(F6/F7) → 错误分类(I7)。
  入向"平台侧"构造均独立实现（D5 纪律，见 tests/test_bdd.py 注释）。

  背景:
    假如 商户使用黄金向量 RSA3072 密钥完成套件配置

  # ---------- S2/S4 出向 ----------

  场景: 发起 L0 下单请求
    当 商户发起 L0 下单请求
    那么 协议头携带 appKey 与冻结时间戳与 32 位十六进制 nonce
    而且 digest 头为 sha-256 加 64 位小写 hex
    而且 digest 头列入签名头
    而且 签名头以 WOP-RSA3072-SHA256 v1/1800 开头
    而且 签名段为 512 字符 base64url

  场景: 无 body 查询请求不携带 digest
    当 商户发起无 body 的 GET 查询请求
    那么 协议头不含 digest 头
    而且 签名头不含 digest 段
    而且 wire body 为空

  场景: 防重放字段存在且 nonce 每次不同
    当 商户以真实随机源连续两次发起 L0 下单请求
    那么 两次构建的 nonce 不同

  场景: 发起 L2 加密请求
    当 商户发起 L2 加密下单请求
    那么 协议头携带 x-wop-encrypt 头且以 L2 前缀开头
    而且 wire body 为含 encrypted 字段的 JSON 信封
    而且 digest 头列入签名头

  场景: 确定性重放逐字节一致
    当 商户以相同时间戳与 nonce 重复构建 L2 请求
    那么 两次草稿逐字节一致

  # ---------- S1 配置 ----------

  场景: 非三段式套件字符串被明确拒绝
    当 商户使用非法套件 WOP-RSA3072 初始化客户端
    那么 抛出 SuiteParseError

  场景: 跨族套件组合被明确拒绝
    当 商户使用非法套件 WOP-RSA3072-SM3 初始化客户端
    那么 抛出 UnsupportedSuiteError
    而且 错误信息包含跨族字样

  场景: 跨族套件反向组合被明确拒绝
    当 商户使用非法套件 WOP-SM2-SHA256 初始化客户端
    那么 抛出 UnsupportedSuiteError

  场景: 非法安全等级被拒绝
    当 商户以非法等级 L3 发起请求
    那么 抛出 ValueError

  # ---------- S5 入向 L0 ----------

  场景: 平台 L0 响应验签通过
    假如 平台独立返回 L0 下单结果响应
    当 商户校验平台响应
    那么 校验通过且明文为下单结果报文

  场景: 响应签名被篡改时模糊拒绝
    假如 平台独立返回 L0 下单结果响应
    当 平台篡改响应签名
    而且 商户校验平台响应
    那么 校验失败且原因为模糊的签名验证失败
    而且 错误类型为 SignatureVerifyError

  场景: 响应摘要与体不符时明确拒绝
    假如 平台独立返回 L0 下单结果响应
    当 平台以错误摘要重新签名响应
    而且 商户校验平台响应
    那么 校验失败且错误类型为 DigestMismatchError

  场景: 响应体缺失 digest 被明确拒绝
    假如 平台独立返回缺少 digest 的响应
    当 商户校验平台响应
    那么 校验失败且错误类型为 DigestMismatchError

  场景: 空响应体携带 digest 被明确拒绝
    假如 平台独立返回空响应体但携带 digest 的响应
    当 商户校验平台响应
    那么 校验失败且错误类型为 ProtocolFormatError

  场景: 缺少 x-wop-sign 头被明确拒绝
    假如 平台独立返回缺少 x-wop-sign 的响应
    当 商户校验平台响应
    那么 校验失败且错误类型为 ProtocolFormatError

  场景: 带等号填充的签名编码被拒绝
    假如 平台独立返回 L0 下单结果响应
    当 平台在签名编码后追加填充符号
    而且 商户校验平台响应
    那么 校验失败且错误类型为 ProtocolFormatError

  场景: 响应声明套件与商户配置不符被拒绝
    假如 平台独立返回 L0 下单结果响应
    当 平台以 RSA4096 套件声明响应
    而且 商户校验平台响应
    那么 校验失败且错误类型为 UnsupportedSuiteError

  # ---------- S6 入向 L2 ----------

  场景: 平台 L2 响应解密成功
    假如 平台独立返回 L2 机密响应
    当 商户校验平台响应
    那么 校验通过且解密明文为机密报文

  场景: L2 密文与 DEK 不一致时模糊拒绝
    假如 平台独立返回 L2 机密响应
    当 平台以错误密钥重组 L2 密文
    而且 商户校验平台响应
    那么 校验失败且原因为模糊的解密失败
    而且 错误类型为 DecryptError

  # ---------- S7 回调 ----------

  场景: 平台支付回调验签通过
    假如 平台独立发送支付回调
    当 商户校验平台回调
    那么 校验通过且明文为支付通知报文

  # ---------- S1/S6 国密 ----------

  场景: SM2 国密套件 L2 全链路
    假如 商户使用黄金向量 SM2 密钥完成套件配置
    而且 平台以底层原语返回国密 L2 响应
    当 商户校验平台响应
    那么 校验通过且解密明文为国密机密报文
